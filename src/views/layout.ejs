<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aplicação</title>
</head>
<body>
    <nav>
        <ul>
            <% if (user && user.isAdmin()) { %>
                <li><a href="/admin/dashboard">Admin Dashboard</a></li>
            <% } %>
            <% if (user && user.isColunista()) { %>
                <li><a href="/colunista/dashboard">Colunista Dashboard</a></li>
            <% } %>
        </ul>
    </nav>
    <div>
        <%- body %>
    </div>
</body>
</html>